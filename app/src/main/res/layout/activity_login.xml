<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LoginActivity">

    <!-- Background image -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_main"
        tools:ignore="ContentDescription" />

    <!-- Overlay layout to ensure elements are on top of the background image -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <!-- Welcome text, centered vertically and horizontally -->

        <!-- Social media login buttons, aligned at the bottom center -->

        <TextView
            android:id="@+id/welcome_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@+id/linearLayout"
            android:layout_marginBottom="367dp"
            android:fontFamily="@font/arizonia"
            android:text="Moul\nWorld of immortality"
            android:textAlignment="center"
            android:textColor="#054750"
            android:textSize="40sp"
            android:textStyle="bold|italic"
            tools:ignore="HardcodedText" />

        <LinearLayout
            android:id="@+id/linearLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="32dp"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/img_btn_google"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="16dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:srcCompat="@drawable/tego"
                tools:ignore="ContentDescription" />

            <ImageButton
                android:id="@+id/img_btn_github"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:srcCompat="@drawable/ic_github"
                tools:ignore="ContentDescription" />

        </LinearLayout>

    </RelativeLayout>

</RelativeLayout>
